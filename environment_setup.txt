Install Linux OS ubuntu 16.04

Install anaconda3, pip, opencv, tensorflow, etc.

#configure GPU environment

Install NVIDIA driver
	note: search on NVIDIA.com to find the version compatable with your GPU

Install CUDA
	version 8.0
Install cudnn
	version 7.1.3 for Linux

#Configure a remote server

Install ngrok
	refer ngrok.com
	note: when start tunnel, use command "ngrok tcp 22". 22 is the default ssh port.	
	
Install openssh-server

Try
